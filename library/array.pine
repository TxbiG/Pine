pub i32 std_first_i32(i32 values[4]) {
    return values[0];
}

pub i32 std_sum4_i32(i32 values[4]) {
    return values[0] + values[1] + values[2] + values[3];
}

pub i32 std_index_i32(i32 values[4], i32 index) {
    return values[index];
}
